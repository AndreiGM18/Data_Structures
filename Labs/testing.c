#include <stdio.h>
#include <stdlib.h>
#include <string.h>
  
int main(void)
{
	// int n, m;
	// scanf("%d%d", &n, &m);
	// int **a = (int **)malloc(n * sizeof(int *));
	// if (!a) {
	// 	printf("malloc failed\n");
	// 	return -1;
	// }
	// for (int i = 0; i < n; ++i) {
	// a[i] = (int *)malloc(m * sizeof(int));
	// if (!a[i]) {
	// 	printf("malloc failed\n");
	// 	return -1;
	// }
	// for (int j = 0; j < m; ++j) {
	// 	scanf("%d", &a[i][j]);
	// 	if (a[i][j] != 0 && a[i][j] != 1)
	// 		--j;
	// 	}
	// }
	// for (int i = 0; i < n; ++i) {
	// 	int nr = 0;
	// 	for (int j = 0; j < m ; ++j)
	// 		nr += a[i][m - 1 - j]<<j;
	// 	printf("%d ", nr);
	// }
	// for (int i = 0; i < n; ++i)
	// 	free(a[i]);
	// free(a);
	// return 0;
}
